<form method="post" action="{{ path('app_suivi_materiel_mobiliers_delete', {'id': suivi_materiel_mobilier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivi_materiel_mobilier.id) }}">
    <button class="btn">Delete</button>
</form>
