<form method="post" action="{{ path('commissaires_comptes_delete', {'id': commissaires_compte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commissaires_compte.id) }}">
    <button class="btn">Delete</button>
</form>
