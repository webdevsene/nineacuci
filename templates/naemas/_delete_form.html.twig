<form method="post" action="{{ path('n_a_e_m_a_s_delete', {'id': n_a_e_m_a.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ n_a_e_m_a.id) }}">
    <button class="btn">Delete</button>
</form>
