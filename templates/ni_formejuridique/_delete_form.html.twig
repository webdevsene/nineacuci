<form method="post" action="{{ path('app_ni_formejuridique_delete', {'id': ni_formejuridique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ni_formejuridique.id) }}">
    <button class="btn">Delete</button>
</form>
