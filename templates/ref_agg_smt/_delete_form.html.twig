<form method="post" action="{{ path('app_ref_agg_smt_delete', {'id': ref_agg_smt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ref_agg_smt.id) }}">
    <button class="btn">Delete</button>
</form>
