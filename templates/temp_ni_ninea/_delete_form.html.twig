<form method="post" action="{{ path('app_temp_n_i_ninea_delete', {'id': temp_n_i_ninea.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ temp_n_i_ninea.id) }}">
    <button class="btn">Delete</button>
</form>
