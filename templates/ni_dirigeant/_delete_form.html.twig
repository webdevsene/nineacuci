<form method="post" action="{{ path('app_ni_dirigeant_delete', {'id': ni_dirigeant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ni_dirigeant.id) }}">
    <button class="btn">Delete</button>
</form>
