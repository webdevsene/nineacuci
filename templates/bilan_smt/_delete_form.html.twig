<form method="post" action="{{ path('app_bilan_smt_delete', {'id': bilan_smt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bilan_smt.id) }}">
    <button class="btn">Delete</button>
</form>
