{% extends 'base.html.twig' %}

{% block title %}Citi{% endblock %}

{% block body %}
    <h1>Citi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ citi.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ citi.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('citi_index') }}">back to list</a>

    <a href="{{ path('citi_edit', {'id': citi.id}) }}">edit</a>

    {{ include('citi/_delete_form.html.twig') }}
{% endblock %}
