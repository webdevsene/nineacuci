{% extends 'base.html.twig' %}

{% block title %}Citi index{% endblock %}

{% block body %}
    <h1>Citi index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for citi in citis %}
            <tr>
                <td>{{ citi.id }}</td>
                <td>{{ citi.libelle }}</td>
                <td>
                    <a href="{{ path('citi_show', {'id': citi.id}) }}">show</a>
                    <a href="{{ path('citi_edit', {'id': citi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('citi_new') }}">Create new</a>
{% endblock %}
