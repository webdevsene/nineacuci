<form method="post" action="{{ path('app_nin_typedocuments_delete', {'id': nin_typedocument.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nin_typedocument.id) }}">
    <button class="btn">Delete</button>
</form>
