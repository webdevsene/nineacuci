{% extends 'base.html.twig' %}

{% block title %}NiSourcefinancement{% endblock %}

{% block body %}
    <h1>NiSourcefinancement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ni_sourcefinancement.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ ni_sourcefinancement.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ni_sourcefinancement_index') }}">back to list</a>

    <a href="{{ path('app_ni_sourcefinancement_edit', {'id': ni_sourcefinancement.id}) }}">edit</a>

    {{ include('ni_sourcefinancement/_delete_form.html.twig') }}
{% endblock %}
