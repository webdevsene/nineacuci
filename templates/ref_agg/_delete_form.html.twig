<form method="post" action="{{ path('ref_agg_delete', {'id': ref_agg.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ref_agg.id) }}">
    <button class="btn"><i class="fa fa-trash "></i></button>
</form>
