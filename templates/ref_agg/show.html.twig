{% extends 'base.html.twig' %}

{% block title %}RefAgg{% endblock %}

{% block body %}
    <h1>RefAgg</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ref_agg.id }}</td>
            </tr>
            <tr>
                <th>Parent</th>
                <td>{{ ref_agg.parent }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ ref_agg.code }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ ref_agg.libelle }}</td>
            </tr>
            <tr>
                <th>Ordre</th>
                <td>{{ ref_agg.ordre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ref_agg_index') }}">back to list</a>

    <a href="{{ path('ref_agg_edit', {'id': ref_agg.id}) }}">edit</a>

    {{ include('ref_agg/_delete_form.html.twig') }}
{% endblock %}
