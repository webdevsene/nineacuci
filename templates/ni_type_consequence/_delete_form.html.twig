<form method="post" action="{{ path('app_ni_type_consequence_delete', {'id': ni_type_consequence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ni_type_consequence.id) }}">
    <button class="btn">Delete</button>
</form>
