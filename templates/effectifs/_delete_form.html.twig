<form method="post" action="{{ path('app_effectifs_delete', {'id': effectif.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ effectif.id) }}">
    <button class="btn">Delete</button>
</form>
