<form method="post" action="{{ path('app_nin_jour_ferier_delete', {'id': nin_jour_ferier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nin_jour_ferier.id) }}">
    <button class="btn btn-danger btn-sm mt-lg-2">Confirmer supression</button>
</form>
