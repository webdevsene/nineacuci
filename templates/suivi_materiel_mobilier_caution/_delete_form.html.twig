<form method="post" action="{{ path('app_suivi_materiel_mobilier_caution_delete', {'id': suivi_materiel_mobilier_caution.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivi_materiel_mobilier_caution.id) }}">
    <button class="btn">Delete</button>
</form>
