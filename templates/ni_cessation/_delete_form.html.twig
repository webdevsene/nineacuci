<form method="post" action="{{ path('app_ni_cessation_delete', {'id': ni_cessation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ni_cessation.id) }}">
    <button class="btn">Delete</button>
</form>
