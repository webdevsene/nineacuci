{% extends 'base.html.twig' %}

{% block title %}SYSCOA{% endblock %}

{% block body %}
    <h1>SYSCOA</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ s_y_s_c_o_a.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ s_y_s_c_o_a.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('s_y_s_c_o_a_index') }}">back to list</a>

    <a href="{{ path('s_y_s_c_o_a_edit', {'id': s_y_s_c_o_a.id}) }}">edit</a>

    {{ include('syscoa/_delete_form.html.twig') }}
{% endblock %}
