{% extends 'base.html.twig' %}

{% block title %}SYSCOA index{% endblock %}

{% block body %}
    <h1>SYSCOA index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for s_y_s_c_o_a in s_y_s_c_o_as %}
            <tr>
                <td>{{ s_y_s_c_o_a.id }}</td>
                <td>{{ s_y_s_c_o_a.libelle }}</td>
                <td>
                    <a href="{{ path('s_y_s_c_o_a_show', {'id': s_y_s_c_o_a.id}) }}">show</a>
                    <a href="{{ path('s_y_s_c_o_a_edit', {'id': s_y_s_c_o_a.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('s_y_s_c_o_a_new') }}">Create new</a>
{% endblock %}
