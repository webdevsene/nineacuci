<form method="post" action="{{ path('actionnaire_delete', {'id': actionnaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actionnaire.id) }}">
    <button class="btn">Delete</button>
</form>
