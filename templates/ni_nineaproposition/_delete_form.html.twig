<form method="post" action="{{ path('ni_nineaproposition_delete', {'id': ni_nineaproposition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ni_nineaproposition.id) }}">
    <button class="btn">Delete</button>
</form>
