<form method="post" action="{{ path('q_v_h_delete', {'id': q_v_h.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ q_v_h.id) }}">
    <button class="btn">Delete</button>
</form>
