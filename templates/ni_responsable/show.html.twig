{% extends 'base.html.twig' %}

{% block title %}NiResponsable{% endblock %}

{% block body %}
    <h1>NiResponsable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ni_responsable.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ ni_responsable.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ni_responsable_index') }}">back to list</a>

    <a href="{{ path('app_ni_responsable_edit', {'id': ni_responsable.id}) }}">edit</a>

    {{ include('ni_responsable/_delete_form.html.twig') }}
{% endblock %}
